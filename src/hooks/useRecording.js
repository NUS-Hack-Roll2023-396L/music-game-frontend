import React, { useState, useEffect } from 'react'
import { AudioRecorder } from 'react-audio-voice-recorder';

export default function useRecording(recording) {
  // const [recording, setRecording] = useState(false)
  useEffect(() => {
  
    // return () => {
    //   second
    // }
  }, [recording])
  
  
}
